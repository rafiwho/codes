#include <bits/stdc++.h>
using namespace std;

void tcase() {
	set<int>st;
	st.contains(12);
	cout << st.contains(12) << '\n';
}
int32_t main() {

	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);

	int32_t t = 1;
	//cin >> t;

	while (t-- > 0)
		tcase();
}