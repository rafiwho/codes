#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void tcase() {
  int n, m; cin >> n >> m;
  vector<pair<set<int>, set<int>>> v(n + 1);
  for(int i = 0; i < m; ++i) {
    int l,r,d; cin >> l >> r >> d;
    v[l].
  }
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    // cin >> t;

    while (t--) {
        tcase();
    }
}
